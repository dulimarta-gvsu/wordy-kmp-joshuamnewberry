import SwiftUI
import composeApp

@main
struct iOSApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

struct WordScreenView: View {
    var body: some View {
        VStack {
            Button("Restart") {

            }
        }
    }
}